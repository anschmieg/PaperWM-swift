# DeskPad Submodule Placeholder

This directory is intended to contain the DeskPad submodule.

To add DeskPad as a submodule, run:

```bash
# First, remove this placeholder directory
rm -rf submodules/DeskPad

# Then add the actual submodule
git submodule add <DESKPAD_REPO_URL> submodules/DeskPad
```

See `SUBMODULE_SETUP.md` in the repository root for detailed instructions.

## Note

This placeholder file exists because Git does not track empty directories. Once you add the actual DeskPad submodule, this file will be replaced with the submodule contents.
